info:
  name: import
  type: http

http:
  method: GET
  url: http://127.0.0.1:8002/api/repositories/import/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
