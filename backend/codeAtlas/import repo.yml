info:
  name: import repo
  type: http
  seq: 1

http:
  method: GET
  url: |-
    POST
    http://127.0.0.1:8001/api/repositories/import/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
